# Set in Vercel Project → Settings → Environment Variables (also at local build time if the app reads them).
# Use your Render (or other) API origin including servlet context path when applicable.
# Example: https://towsoth-backend.onrender.com/api
VITE_API_BASE_URL=
VITE_APP_ENV=production
